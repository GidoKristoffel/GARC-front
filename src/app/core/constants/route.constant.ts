import { EPage } from "../enums/page.enum";

export const baseUrl: string = EPage.Main + '/';
export const baseUrlAccount: string = EPage.Main + '/' + EPage.Account + '/';
export const baseUrlCalculation: string = EPage.Main + '/' + EPage.Calculation + '/';
