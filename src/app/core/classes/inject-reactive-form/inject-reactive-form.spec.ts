import { InjectReactiveForm } from './inject-reactive-form';

describe('InjectReactiveForm', () => {
  it('should create an instance', () => {
    expect(new InjectReactiveForm()).toBeTruthy();
  });
});
